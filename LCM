#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    int max = (a > b) ? a : b;
    int lcm = max;

    while(true) {
        if(lcm % a == 0 && lcm % b == 0)
            break;
        lcm++;
    }

    cout << lcm;
    return 0;
}
