#include <iostream>
using namespace std;

int main() {
    int num, i = 1, sum = 0;
    cin >> num;

    while(i < num) {
        if(num % i == 0)
            sum += i;
        i++;
    }

    if(sum == num)
        cout << "Perfect";
    else
        cout << "Not Perfect";

    return 0;
}
