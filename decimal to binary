#include <iostream>
using namespace std;

int main() {
    int num, bin[32], i = 0;
    cin >> num;

    while(num > 0) {
        bin[i++] = num % 2;
        num /= 2;
    }

    while(i--)
        cout << bin[i];

    return 0;
}
